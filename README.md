# operator

Run this on the target Ubuntu machine:

```bash
curl -fsSL https://github.com/techsudoku-com/operator/releases/download/init/install.sh | sudo bash
```


```bash

ts-cli status

ts-cli --help

```
